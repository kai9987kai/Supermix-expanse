# Tested with: Python 3.12, torch 2.11 (CPU), numpy 2.4, scipy 1.17, gradio 6.28 on Windows 11 ARM64.
torch>=2.4
numpy>=1.26
scipy>=1.11
scikit-learn>=1.3
safetensors>=0.4
tokenizers>=0.19
huggingface_hub>=0.25
sympy>=1.12
pyarrow>=14          # only to rebuild the connectome graph from the raw feathers
datasets>=2.18       # only to (re)download PubMedQA for teacher data
gradio>=5.0          # chat UI
pytest>=8
